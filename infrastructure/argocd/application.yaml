apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ai-speaking-platform
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: 'https://github.com/alex/ai-speaking.git'
    targetRevision: main
    path: infrastructure/k8s
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: ai-platform
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
